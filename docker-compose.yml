services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      - CORS_ORIGINS=["http://localhost","http://localhost:80","https://sprxenroll-iq-app-zlf3bk-b82472-72-60-20-16.traefik.me","http://sprxenroll-iq-app-zlf3bk-b82472-72-60-20-16.traefik.me"]
